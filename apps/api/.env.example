# APP
API_PORT=3000
API_BASE_URL=http://localhost:3000

BETTER_AUTH_SECRET=your_secret_here_min_32_chars_use_openssl_rand_base64_32
CLIENTS_WEB_APP_URL=http://localhost:5174
TRUSTED_ORIGINS=http://localhost:3000,http://localhost:5174

# DATABASE
DATABASE_PASSWORD=rostilesk
DATABASE_USER=lesk
DATABASE_NAME=rosti_db
DATABASE_HOST=localhost
DATABASE_PORT=5111

# Email Configuration (MailDev)
EMAIL_HOST=localhost
EMAIL_PORT=1025
EMAIL_SECURE=false
EMAIL_FROM=noreply@localhost.com

# Sentry
SENTRY_DSN=

# Avatar files. Leave unset in development (stored in data/avatars).
# In production, mount a volume at /data/avatars. That path is the default there.
# AVATAR_STORAGE_DIR=

# Optional feature flags. isFeatureEnabled('example') reads FEATURE_EXAMPLE.
# FEATURE_EXAMPLE=true
